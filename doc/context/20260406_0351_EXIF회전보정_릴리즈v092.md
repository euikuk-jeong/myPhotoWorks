# 세션 요약 — 2026-04-06 03:51

## 작업 목록

- **EXIF Orientation 태그 기반 이미지 자동 회전 보정 구현 (v0.9.2)**
  - 초기: 높이/너비 비교로 세로 이미지 감지 → 스플리터 방향 전환 시도
  - 재정의: EXIF 태그만 사용, 레이아웃은 좌→우 유지, 픽셀만 보정
  - `ImageOps.exif_transpose` 적용 위치:
    - `preview_window.py` — `_open_image()` 헬퍼 추출, prefetch/동기로드/render_before 폴백 전체
    - `thumbnail_panel.py` — 메인 화면 썸네일
    - `processor.py` — 배치 처리 폴백 경로 및 저장 시 Orientation 태그 → 1(Normal) 초기화
- **이중 회전 버그 수정**: 저장된 JPEG의 Orientation 태그가 남아 뷰어가 재회전하는 문제 수정
- **단위 테스트 추가**: `TestExifTransposeOnLoad` (preview_window), `test_save_resets_orientation_to_normal` (processor)
- **doc/context git 추적 허용**
  - `.gitignore`에서 `/doc/context/` 제외 규칙 삭제
  - `CLAUDE.md` Git Rules 업데이트
- **v0.9.2 릴리즈**
  - `pyproject.toml` / `uv.lock` 버전 0.9.1 → 0.9.2
  - `git tag v0.9.2` 생성 및 push
  - GitHub Release 생성 (`gh release edit v0.9.2`)

## 주요 결정 사항

- EXIF 회전 감지는 **높이/너비 비교 금지 — EXIF Orientation 태그만 사용**
- 레이아웃(스플리터 방향)은 **항상 Horizontal(좌→우)** 유지
- `exif_transpose`는 이미지 **로드 시점에 한 번만** 적용
- 저장 시 Orientation 태그를 **반드시 1(Normal)로 초기화** — 미적용 시 이중 회전 발생

## 커밋 이력 (이번 세션)

| 커밋 | 내용 |
|---|---|
| `651fefb` | Release v0.9.2: EXIF Orientation 기반 이미지 자동 회전 적용 |
| `48776b9` | Fix double-rotation on save: reset EXIF Orientation to Normal |
| `0d8b66e` | Add doc/context to git tracking and update rules |

## 현재 브랜치 상태

- `main`: v0.9.2 모든 변경사항 반영, 태그 및 GitHub Release 완료
- `second_stage`: main의 변경사항 미반영 상태 (세션 종료 시점 기준)

## 다음 단계 (있을 경우)

- `second_stage` 브랜치에 main 변경사항 병합 여부 검토
