# 미리보기 성능 최적화

- **날짜**: 2026-03-29 13:23
- **브랜치**: first_stage
- **커밋**: 61c8df0

## 작업 목록

- 미리보기 화면의 각 단계별 타이밍 로그(DEBUG 레벨) 추가
  - `_load_current()`: Image.open, downsample, EXIF, render_before, render_after_A/B, TOTAL
  - `processor.process()`: Image.open/cache copy, auto_level, auto_contrast, brightness/contrast, resize, TOTAL
  - `_save_and_advance()`: process, write, TOTAL
  - 이펙트 변경 시 re-render 시간
- `main.py`에 `logging.basicConfig` 설정 추가, PIL DEBUG 로그 필터링(WARNING)
- 원본 이미지 캐싱: `Image.open()` 호출 3회→1회로 감소
- **미리보기용 다운샘플링**: 40MP 원본 대신 long side 1600px로 축소 후 이펙트 적용 (~10배 속도 개선)
- `processor.process()`에 `source_image` 파라미터 추가 (디스크 I/O 스킵)
- 테스트 추가: `TestProcessSourceImage` (3건), `TestMakePreviewImage` (3건)

## 주요 결정 사항

- 미리보기 해상도 상한: `_PREVIEW_MAX_PX = 1600` (long side 기준)
  - 미리보기 pane 크기(~500px) 대비 충분한 품질 확보
  - 이펙트 처리 대상 픽셀 수: 40MP → ~1.7MP (약 24배 감소)
- 저장 시에는 여전히 디스크에서 원본을 읽어 풀 해상도로 처리 (품질 유지)
- 캐시 이미지는 `_render_before`에서 read-only로 사용, `_render_after`에서는 `process()` 내부에서 `.copy()` 후 이펙트 적용

## 성능 측정 결과 (X-T5, 7728x5152)

| 항목 | 최적화 전 | 최적화 후 (예상) |
|------|----------|----------------|
| auto_level | ~1800ms | ~75ms |
| auto_contrast | ~1300ms | ~55ms |
| _load_current TOTAL | ~5100ms | ~500ms 이하 |

## 다음 단계

- 실제 최적화 후 로그로 성능 수치 확인 필요
- 슬라이더 조작 시 debounce 적용 검토 (빠른 연속 조작 시 불필요한 re-render 방지)
- 저장 시 성능도 캐시 활용으로 개선 가능 (현재는 디스크에서 다시 읽음)
